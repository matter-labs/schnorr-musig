{
    "name": "musig",
    "main": "build/signer.js",
    "types": "build/signer.d.ts",
    "license": "MIT",
    "version": "0.1.0",
    "dependencies": {
        "ethers": "5.0.12",
        "musig-bindings": "link:../musig-bindings",
        "zksync-crypto": "^0.3.2"
    },
    "devDependencies": {
        "@types/chai": "^4.2.12",
        "@types/mocha": "^8.0.3",
        "@types/node": "^14.6.4",
        "chai": "^4.2.0",
        "mocha": "^8.1.3",
        "prettier": "^2.1.1",
        "ts-node": "^9.0.0",
        "tslint": "^6.1.3",
        "typescript": "^4.0.2"
    },
    "scripts": {
        "build": "tsc -b",
        "clean": "rm -rf build/*",
        "test": "mocha --no-timeout -r ts-node/register test/*.test.ts --exit",
        "lint": "tslint -c tslint.json {src,test}/*.ts",
        "fmt": "prettier --tab-width 4 --print-width 120 --parser typescript --write {src,test}/*.ts"
    }
}
